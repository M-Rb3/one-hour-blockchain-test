{
  "name": "one-hour-blockchain-test",
  "version": "1.0.0",
  "description": "One-hour blockchain coding test template with Solidity smart contracts and tests",
  "scripts": {
    "postinstall": "cd frontend && npm install --no-audit --no-fund || true",
    "test": "npx hardhat test",
    "dev": "echo '\\n📋 To run tests: npm test\\n🌐 To start frontend: cd frontend && npm run dev\\n'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "keywords": ["blockchain", "solidity", "hardhat", "test", "ethereum"],
  "license": "MIT"
}